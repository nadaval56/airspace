# מפרט פרויקט — מפת מגבלות מרחב אווירי, מטה בנימין

מסמך זה מיועד להרצה ב-Claude Code כנקודת פתיחה לפרויקט.

---

## 1. מה בונים

אתר סטטי אחד ב-GitHub Pages שמציג מפה של אזור מטה בנימין ועליה שתי שכבות:

1. **מגבלות קבועות** — אזורים אסורים, מוגבלים ומסוכנים לטיסה, מתוך פמ"ת פרק א-17 (נספחים ב' ו-ג').
2. **מגבלות זמניות** — נוטאמים פעילים של מרחב תל אביב (LLLL), מתעדכנים אוטומטית כל שעה.

המשתמש נכנס לאתר ביוזמתו כשהוא רוצה לדעת מה המצב. אין התראות.

## 2. מה לא בונים

הגבולות האלה מכוונים, אין לחרוג מהם:

- **אין Claude API** ואין שום קריאה למודל שפה. הפרסור הוא לוגיקה רגילה.
- **אין וואטסאפ**, אין מייל, אין שום ערוץ התראה.
- **אין סינון נוטאמים.** מציגים את כולם. הנפח קטן (סדר גודל של 12 פעילים), ומגבלה חריגה בגובה נמוך היא בדיוק מה שהמשתמש מחפש — מסנן עלול לבלוע אותה. מותר לסמן ויזואלית מה קרוב לקרקע, אסור להסתיר.
- **אין מסד נתונים**, אין שרת, אין build step. HTML/CSS/JS גולמי.

## 3. ארכיטקטורה

```
GitHub Actions (cron, כל שעה)
        ↓
  סקריפט Python: משיכה + פרסור
        ↓
  data/notams.json  ←  commit ל-repo
        ↓
GitHub Pages: index.html קורא JSON סטטי
```

הדף לא פונה למקור חיצוני בזמן טעינה. הסיבה: מקורות הנוטאמים חוסמים CORS. עיכוב של עד שעה סוכם עם המשתמש כמקובל.

**חשוב:** ה-repo חייב להיות ציבורי — ב-repo ציבורי דקות Actions הן חינם ובלתי מוגבלות. ב-repo פרטי המכסה היא 2,000 דקות בחודש לכל החשבון, וזה יפגע בפרויקטים אחרים של המשתמש.

שים לב: ב-repo ציבורי חינמי, cron מושבת אוטומטית אחרי 60 יום ללא פעילות ב-repo. הוסף הערה על כך ב-README.

## 4. מקור הנוטאמים

מקור ראשי: `https://notams.online/icao/LLLL`

אומת ידנית — מחזיר נוטאמים אמיתיים, חינם, ללא הרשמה, כ-12 פעילים.

**שלב ראשון לביצוע:** בדוק אם יש endpoint שמחזיר JSON מאחורי הדף (התוכן נטען דינמית, אז סביר שכן). אם יש — השתמש בו. אם לא — פרסר HTML עם BeautifulSoup.

הוסף גם משיכה לשדות התעופה: `LLBG`, `LLHA`, `LLIB`, `LLRD`, `LLHZ`. נוטאמים ברמת שדה לפעמים כוללים הגבלות מקומיות שלא מופיעות ברמת ה-FIR. עלות אפסית.

**עמידות:** אם המשיכה נכשלת, אל תדרוס את `notams.json` הקיים. במקום זה עדכן שדה `last_error` ו-`last_success` בקובץ, והדף יציג את הנתונים הישנים עם אזהרה בולטת על גילם. נתון ישן עם חותמת זמן כנה עדיף על דף ריק.

## 5. פרסור שורת Q

זה הלב הטכני. שורת Q בנוטאם נראית כך:

```
Q) LLLL/QRTCA/IV/NBO/W/000/999/3155N03518E010
```

השדות מופרדים בלוכסן, לפי הסדר:

| # | שדה | דוגמה | משמעות |
|---|------|--------|---------|
| 1 | FIR | `LLLL` | מרחב |
| 2 | קוד Q | `QRTCA` | נושא ומצב |
| 3 | תעבורה | `IV` | IFR/VFR |
| 4 | מטרה | `NBO` | תפוצה |
| 5 | היקף | `W` | A=שדה, E=נתיב, W=אזהרה |
| 6 | גובה תחתון | `000` | במאות רגל |
| 7 | גובה עליון | `999` | במאות רגל |
| 8 | מיקום+רדיוס | `3155N03518E010` | ראה למטה |

**פרסור שדה 8:** `DDMM` + `N/S` + `DDDMM` + `E/W` + `RRR`

בדוגמה: 31°55'N, 035°18'E, רדיוס 10 מייל ימי. ההמרה לעשרוני: `DD + MM/60`.

**מקרה קצה חשוב:** רדיוס `999` פירושו כל ה-FIR. אל תצייר עיגול בקוטר 2000 מייל — הוא יבלע את המפה. נוטאמים כאלה מוצגים כתג ברשימה בלבד, עם סימון "חל על כל המרחב".

גם שדות `000/999` בגבהים פירושם "ללא הגבלת גובה", לא "מ-0 עד 99,900 רגל".

**גיבוי:** אם שורת Q חסרה או פגומה, אל תפיל את הרשומה. שמור אותה עם `geo: null` — היא תופיע ברשימה מתחת למפה בלי סמן על המפה.

## 6. שכבת הפמ"ת

עבודה חד-פעמית, ידנית ברובה.

מקור: `https://www.gov.il/BlobFolder/guide/aip/he/aip_א'-17.pdf`

- **נספח ב'** — קואורדינטות אזורים אסורים, מוגבלים ומסוכנים לטיסה
- **נספח ג'** — קואורדינטות אזורים אסורים לטיסת כטב"ם (הרלוונטי ביותר לרחפנים)

חלץ את הטבלאות, המר ל-GeoJSON, ושמור ב-`data/aip-permanent.geojson`.

**חתוך רק לתיבה:** 31.7–32.1°N, 35.0–35.4°E. כל הארץ תהפוך את המפה לבלתי קריאה.

לכל פוליגון שמור: מזהה, שם, סוג (אסור/מוגבל/מסוכן/כטב"ם), טווח גבהים, והערות.

**זהו הנתון החשוב יותר מבין השניים.** הוא מדויק, יציב, ובשליטה מלאה. שכבת הנוטאמים היא תוספת מעליו. אם נגמר הזמן — עדיף אתר עם פמ"ת בלבד מאשר אתר עם נוטאמים בלבד.

## 7. הדף

מפת Leaflet, ממורכזת על כוכב השחר (31.9558°N, 35.3392°E), זום התחלתי 11.

**שכבות:**
- פוליגוני פמ"ת — מילוי שקוף למחצה, מתאר מלא. צבע לפי סוג.
- עיגולי נוטאם — מתאר מקווקו, בולטים מעל הפמ"ת. הבחנה ויזואלית בין נוטאמים שהרצפה שלהם מתחת ל-3,000 רגל לבין הגבוהים — אבל שניהם מוצגים.
- מתג הצגה/הסתרה לכל שכבה.

**לחיצה על אלמנט** פותחת חלונית עם: מזהה, זמני תוקף, טווח גבהים, וטקסט מלא. הצג גם את הנוטאם הגולמי מתחת, מקופל.

**מתחת למפה** — רשימה מלאה של כל הנוטאמים הפעילים, כולל אלה בלי מיקום גיאוגרפי. ממוינת לפי זמן פרסום, החדש למעלה.

**בראש הדף** — חותמת זמן העדכון האחרון בעברית ובזמן מקומי, בגודל שאי אפשר לפספס.

**באנר NFOU** קבוע ובולט:

> מידע זה אינו מיועד לשימוש מבצעי. אינו מחליף תדריך רשמי או תיאום מול הגורמים המוסמכים.

זה לא נוסח משפטי לעיטור — הכלי מסתמך על מקור צד שלישי לא רשמי, ופעילות מקומית בגובה נמוך לרוב אינה מופצת בערוץ הבינלאומי כלל. משמעותית: **היעדר נוטאם באתר אינו ראיה להיעדר מגבלה.**

**עיצוב:** עברית, RTL, מובייל-פירסט. המשתמש ייכנס מהטלפון בשטח. הימנע מפלטת ברירת המחדל של Leaflet — בחר כיוון ויזואלי מכוון. קריאות בשמש חשובה יותר מאלגנטיות.

## 8. מבנה ה-repo

```
├── .github/workflows/fetch-notams.yml
├── scripts/
│   ├── fetch_notams.py
│   └── parse_qline.py
├── data/
│   ├── notams.json
│   └── aip-permanent.geojson
├── index.html
├── assets/
│   ├── style.css
│   └── app.js
└── README.md
```

## 9. סדר עבודה מומלץ

1. בדיקת ה-endpoint של notams.online וכתיבת המשיכה
2. פרסור שורת Q + בדיקות יחידה על הדוגמאות מסעיף 5
3. workflow של Actions וייצור `notams.json` ראשון
4. המרת נספחי הפמ"ת ל-GeoJSON
5. הדף והמפה
6. README עם הסבר על מגבלות המקור

## 10. הערות למפתח

- כל הזמנים בנוטאמים ב-UTC. הצג בזמן מקומי אבל שמור UTC בקובץ.
- `PERM` בשדה סיום = תוקף קבוע, לא תאריך.
- `EST` אחרי תאריך = משוער.
- `NOTAMR` מחליף נוטאם קודם, `NOTAMC` מבטל. ודא שנוטאם שהוחלף לא מוצג פעמיים.
- נוטאמי `CHECKLIST` ו-`TRIGGER NOTAM` הם מנהלתיים. אל תסנן אותם, אבל מותר לסמן אותם בתג "מנהלתי" כדי שיירדו לתחתית הרשימה.
